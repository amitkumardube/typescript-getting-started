// explicit  type
let uname: string;
uname = 'abc';
console.log(uname);

// arrays
let mixed : (string | number | boolean)[] = [];

mixed.push('abc');
mixed.push(10);
mixed.push(true);

console.log(mixed);